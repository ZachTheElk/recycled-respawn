import { DEVELOPMENT_FIXTURE_ID } from './content-foundation';

// Homepage placement is curated here rather than stored in article frontmatter.
export const homepageCuration = {
	featuredStory: DEVELOPMENT_FIXTURE_ID,
} as const;
